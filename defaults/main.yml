---
# Whether a Miniconda environment should be installed
python3_virtualenv_miniconda: false

# The prefix under which the Miniconda environment should be installed
python3_virtualenv_prefix: '/opt/miniconda'